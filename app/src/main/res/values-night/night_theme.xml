<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.LevelUpGamer" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Colores principales -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Fondo y texto (modo claro) -->
        <item name="android:windowBackground">#FFFFFF</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textColorHint">#333333</item>

        <!-- Botones -->
        <item name="android:buttonStyle">@style/NeonButtonLight</item>

        <!-- ActionBar -->
        <item name="android:actionBarStyle">@style/NeonActionBarLight</item>
    </style>

    <!-- Botón estilo neón sobre fondo claro -->
    <style name="NeonButtonLight" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/colorPrimary</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">10dp</item>
    </style>

    <!-- ActionBar modo claro -->
    <style name="NeonActionBarLight" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">#E6E6E6</item>
        <item name="android:titleTextStyle">@style/NeonActionBarTitleLight</item>
    </style>

    <!-- Título verde neón en ActionBar -->
    <style name="NeonActionBarTitleLight" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
